# Plan

## 1. Дата начала — date picker
- [x] Заменить text input на shadcn/ui `Calendar` + `Popover`
- [x] Починить баг фильтра: задачи на сегодня не показываются (`isBefore` → `!isAfter`)
<!-- - [ ] Определиться с `endDate`: убрать или задействовать -->

<!-- ## 2. Отображение по времени -->
<!-- - [ ] Верифицировать логику фильтра: без даты = inbox (всегда видны), с датой — видны с этой даты -->
<!-- - [ ] Добавить вторичную сортировку по дате (сейчас только по приоритету) -->
<!-- - [ ] Опционально: группировка "сегодня / завтра / позже" -->

## 3. Повторяющиеся задачи
- [x] Поле `repeat` уже есть (`week | month | year`), логики нет
- [x] При выполнении: создаётся новый экземпляр со следующей датой, оригинал помечается `done` (история сохраняется)

## 4. Зависимые задачи
- [x] Добавить поле `dependsOn: string[]` в модель `Todo`
- [x] Задача скрыта/заблокирована, пока не выполнены все зависимости
- [x] В форме редактирования — выбор задач из списка

## 5. Миграция на Temporal API
- [ ] Дождаться нативной поддержки (ожидается ES2026)
- [ ] Заменить date-fns на `Temporal.PlainDate` (parse, format, add для повторений)

## 6 Фиксы
- [x] При удалении корректно сдвикать активный указатель, то сбрасывается на первый
- [ ] Автобэкапы при запуске и завершении
- [ ] Проверка просроченности: объединять `date` + `time` перед сравнением с `new Date()` (иначе задача на 14:00 просрочена с утра)
